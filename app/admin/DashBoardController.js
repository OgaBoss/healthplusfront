'use strict';
/**
 * controllers used for the dashboard
 */

app.controller('DashBoardController', ['$scope','$auth','$state','$localStorage','country','healthNotify', 'Entity',
    function($scope, $auth, $state, $localStorage, country, healthNotify, Entity) {

    }
]);
